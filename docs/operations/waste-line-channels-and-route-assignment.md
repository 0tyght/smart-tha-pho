# LINE และการกำหนดเส้นทางรถเก็บขยะ

## ช่องทาง LINE

ระบบใช้ LINE Official Account `Smart Tha Pho` เพียงบัญชีเดียวสำหรับทุกบริการ ระบบแยกเมนูและสิทธิ์จากบทบาทที่ยืนยันไว้ในฐานข้อมูล

| ผู้ใช้ | Webhook | เมนู |
|---|---|---|
| ประชาชน | `/api/line/webhook` | ลงทะเบียนผู้ใช้บริการเก็บขยะ ตรวจวันเก็บขยะ ติดตามรถ ตรวจค่าบริการ และแจ้งเหตุ |
| พนักงานประจำรถขยะ | `/api/line/webhook` | ยืนยันตัวตน ดูงานวันนี้ เริ่มงาน ส่งตำแหน่ง แจ้งเหตุ และจบงาน |

กำหนด `LINE_CHANNEL_SECRET`, `LINE_CHANNEL_ACCESS_TOKEN` และ `LINE_CHANNEL_ID` ของ Smart Tha Pho เพียงชุดเดียว

## การผูกบ้านกับเส้นทาง

1. ประชาชนลงทะเบียนผ่าน LINE และส่งตำแหน่งบ้าน
2. ระบบบันทึกบ้านเป็น `UNASSIGNED` โดยยังไม่เดาเส้นทางให้อัตโนมัติ
3. หน้า **ผู้ใช้บริการ** คำนวณระยะจากบ้านถึงแนวเส้นทางที่เปิดใช้งาน และเรียงสามเส้นทางที่ใกล้ที่สุด
4. เจ้าหน้าที่ตรวจผลบนแผนที่และกด **ยืนยันเส้นทางนี้**
5. ระบบบันทึกสถานะ `CONFIRMED` พร้อมระยะทาง ผู้ยืนยัน และเวลา จากนั้นสร้างหรือปรับจุดเก็บของบ้านบนเส้นทางให้อัตโนมัติ

แนวทางนี้ลดงานเลือกจากรายการยาว แต่ยังป้องกันการกำหนดผิดจาก GPS คลาดเคลื่อน ถนนซอยแคบ หรือจุดกลับรถที่รถเก็บขยะเข้าไม่ได้

## ข้อมูลเส้นทางตามประกาศ

ไฟล์ `database/demo/waste_tha_pho_routes.sql` มีเส้นทางรถทั้ง 6 คัน พร้อมวัน เวลา และพื้นที่เก็บทั้งหมดที่ถอดจากประกาศการเก็บและขนขยะ ลงวันที่ 24 พฤศจิกายน 2566 ซึ่งเป็นฉบับล่าสุดที่ค้นพบจากเว็บไซต์เทศบาล ณ วันที่ 13 สิงหาคม 2569

## การจัดลำดับจุดเก็บและคำนวณรอบวิ่ง

1. ระบบดึงพิกัดจาก `waste_route_stops` ของเส้นทางที่เลือก ไม่ให้เจ้าหน้าที่วาดแนวเส้นเอง
2. ช่วงพัฒนาระบบส่งเฉพาะละติจูดและลองจิจูดไปยัง Public OSRM Trip โดยไม่ส่งชื่อ ที่อยู่ เบอร์โทรศัพท์ หรือ LINE User ID
3. OSRM จัดลำดับจุดเก็บและคืน GeoJSON ที่วิ่งตามโครงข่ายถนน ระบบบันทึกเป็นข้อเสนอชั่วคราวอายุ 30 นาที
4. หน้าเจ้าหน้าที่แสดงระยะทาง เวลา แนวเส้น และลำดับจุดเก็บ แต่ยังไม่เปลี่ยนข้อมูลปฏิบัติงาน
5. เมื่อเจ้าหน้าที่กด **ยืนยันเส้นทางนี้** ระบบตรวจว่าจุดเก็บไม่ถูกเปลี่ยนระหว่างตรวจสอบ แล้วจึงบันทึกลำดับและแนวเส้นจริงพร้อม Audit Log

แนวเส้นบนแผนที่เก็บ geometry แบบเต็มไว้เป็นแคช ห้ามลดจุดพิกัดก่อนแสดงผลเพราะจะทำให้แผนที่ลากเส้นลัดข้ามถนน ประกาศต้นฉบับระบุชื่อพื้นที่แต่ไม่มีพิกัดหรือไฟล์ GIS เจ้าหน้าที่จึงยังต้องตรวจถนนซอย จุดกลับรถ และข้อจำกัดรถก่อนยืนยัน สำหรับระบบใช้งานจริงควรเปลี่ยน `ROUTING_API_BASE_URL` ไปยัง OSRM ที่เทศบาลควบคุมเอง

